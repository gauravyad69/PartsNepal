ktor:
    application:
        modules:
            - np.com.parts.ApplicationKt.module
    deployment:
        port: 9090
jwt:
  secret: "SKAVIN^69"
  issuer: "parts.com.np"
  audience: "*"
  realm: "Access to 'hello'"

db:
  mongo:
    user: "gauravmongo"
    password: "gaurav"
    host: "cluster0.t9kf18u.mongodb.net"
    port: "27017"
    maxPoolSize: 20
    database:
      name: "partsnepal"
#the db here is not being used, the uri is directly set in the Databases.kt